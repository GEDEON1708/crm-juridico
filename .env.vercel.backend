# Variáveis de Ambiente - Backend
# Copie e cole cada linha na Vercel

# 1. URL do Banco de Dados
DATABASE_URL=postgresql://usuario:senha@host:5432/database?schema=public

# 2. Chave JWT (gere uma nova usando: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_SECRET=SUBSTITUA_POR_UMA_CHAVE_ALEATORIA_DE_64_CARACTERES

# 3. Chave JWT Refresh (diferente da anterior)
JWT_REFRESH_SECRET=SUBSTITUA_POR_OUTRA_CHAVE_ALEATORIA_DE_64_CARACTERES

# 4. Ambiente
NODE_ENV=production

# 5. Porta
PORT=3001

# 6. CORS (adicionar depois do deploy do frontend)
CORS_ORIGIN=https://seu-frontend.vercel.app
